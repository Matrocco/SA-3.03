@version: 3.29
source s_net {
    network(
        transport("udp")
        port(514)
    );
    network(
        transport("tcp")
        port(601)
    );
};

destination d_local {
    file("/var/log/syslog-ng/messages.log");
};

log {
    source(s_net);
    destination(d_local);
};